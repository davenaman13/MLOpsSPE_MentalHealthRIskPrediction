version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - ./backend:/app
    depends_on:
      - redis
    networks:
      - mental-health-net

  frontend:
    build: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend
    networks:
      - mental-health-net

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - mental-health-net

networks:
  mental-health-net:
    driver: bridge